services:
  mongodb:
    image: mongo:7
    container_name: sentinelchat-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - sentinelchat_mongo_data:/data/db

volumes:
  sentinelchat_mongo_data:
